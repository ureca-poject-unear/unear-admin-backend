server:
  port: 8082
  servlet:
    session:
      cookie:
        same-site: none
        secure: true

spring:
  jpa:
    open-in-view: false

  config:
    import:
      - optional:application-secret.yml

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
